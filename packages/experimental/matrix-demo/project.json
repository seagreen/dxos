{
  "sourceRoot": "packages/experimental/matrix-demo/src",
  "projectType": "library",
  "targets": {
    "bundle": {
      "executor": "@nx/vite:build",
      "options": {
        "outputPath": "packages/experimental/matrix/out/matrix"
      },
      "outputs": [
        "{options.outputPath}"
      ]
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "options": {
        "buildTarget": "matrix-demo:bundle"
      }
    }
  }
}
